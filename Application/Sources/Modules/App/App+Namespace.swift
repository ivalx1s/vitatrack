import Foundation

extension VTApp {
    enum Business {
    }

    enum UI {
        enum Membrane {}

        enum NoInternetAccess {}
    }

    enum Settings {
        enum Data {
            enum DBScheme_1_0 {
                enum Model {}
            }
        }
        enum Business {
            enum Model {}
        }

        enum UI {
        }
    }

    enum Config {
        enum Data {
        }
        enum Business {
            enum Model {}
        }
        enum UI {}
    }
}
